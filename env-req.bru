meta {
  name: env-req
  type: http
  seq: 1
}

post {
  url: https://echo.usebruno.com
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{name}}",
    "role": "{{role}}"
  }
}

assert {
  res.status: eq 200
}
